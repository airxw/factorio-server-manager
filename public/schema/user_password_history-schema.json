{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gameserver-panel/public/schema/user_password_history-schema.json",
  "title": "User Password History Schema",
  "description": "用户密码历史表数据契约。用于改密时防止重用最近 N 次密码。来源：s0103 融合定稿 v3.4.0 §互斥点④ B 路径。",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "记录主键 UUID"
    },
    "user_id": {
      "type": "string",
      "format": "uuid",
      "description": "用户 ID（FK → users.id）。用户软删除时保留外键记录"
    },
    "password_hash": {
      "type": "string",
      "minLength": 1,
      "description": "历史密码哈希（bcrypt）。仅用于改密时与候选新密码比对，不可逆推明文"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "密码设置时间（即该密码生效的开始时间）"
    }
  },
  "required": ["id", "user_id", "password_hash", "created_at"]
}
