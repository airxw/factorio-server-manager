---
type: audit
title: v4.13.0 方案 GN-004 独立审查结论
date: 2026-07-24
status: completed
version_target: v4.13.0
reviewer: GN-004（独立审查能力承载者，general_purpose_task）
review_object: docs/plans/v4.13.0-instances-split-plan.md
related:
  - current-note.md
  - .trae/documents/20260724_v4.12.0_角色门控审计.md
  - docs/plans/three-tier-operation-logic-rebuild-plan.md
  - .trae/rules/rules-0.md
tags: [gn004, independent-review, v4.13.0, instances-split, contract-extension]
---

# v4.13.0 方案 GN-004 独立审查结论

## 一、审查元信息

| 字段 | 值 |
|------|---|
| 审查时间 | 2026-07-24 |
| 审查对象 | `docs/plans/v4.13.0-instances-split-plan.md`（v4.13.0 实例详情页拆分与 GM Workbench 后端接入方案） |
| 审查者 | GN-004（独立审查能力承载者，由 general_purpose_task 承载，仅审查不写代码） |
| 审查依据 | rules-0 §四-4/§四-8/§四-10/§四-11/§四-13、rules-3（三层契约）、v4.11.0 假闭合教训、v4.12.0 角色门控审计 |
| 审查范围 | A.方案完整性 / B.依赖DAG与并行策略 / C.闭合判据 / D.subagent台账 / E.public契约保护 / F.假闭合风险 / G.安全与门控 / H.风险评估 |
| 总结论 | **警示放行**（11 项警示，0 项阻断） |

> 本审查为 subagent 上下文执行。审查产出仅一份文档，不修改任何源码文件。主线程在合流前须复核本审查结论并对警示项作出补强或显式知悉。

---

## 二、逐项核查结果

### A. 方案完整性

#### A1. 方案是否覆盖三大目标？

**判定：通过**

**证据**：
- 目标1（实例详情页三视图拆分）：步骤 8-14 覆盖，分别对应 `/admin/servers/:id`、`/store/servers/:id`、`/guild/servers/:id` 三套视图 + 共享子组件抽离 + 旧路径重定向 + 门控上提。
- 目标2（GM Workbench 后端 API 接入）：步骤 15-22 覆盖，包含玩家列表（CRM）、玩家操作、流水报表、时长统计、服主实例列表五个 API + 前端 client 扩展 + 占位页替换 + 路由门控。
- 目标3（Instance_Assets 契约扩展）：步骤 1-7 覆盖，包含契约变更申请、数据契约扩展、数据库迁移、配置契约更新、Mock 同步、AssetService 扩展、前端 API 扩展。

**建议**：无。

---

#### A2. 36 步是否有遗漏或冗余？

**判定：警示**

**证据**：
- v4.12.0 遗留项已全部覆盖：
  - `/instances/:id` 拆分三套视图 ✅（步骤 8-14）
  - `/instances/:id/business` 门控提升至 `instance_admin+` ✅（步骤 9 + 步骤 14）
  - `/instances/:id/player-histories` 门控提升至 `server_admin+` ✅（步骤 8 + 步骤 14）
  - GM Workbench 后端 API 接入 ✅（步骤 15-22）
  - Instance_Assets 契约扩展 ✅（步骤 1-7）
- **遗漏1**：根据 `.trae/rules/bb.md` 规则，"版本更新的详细说明需要同时更新主目录下的 version.md 文件；重大功能或者文件结构调整，要更新主目录下 readme.md 文件"。本方案是路由结构重大调整 + 全新功能（GM Workbench API），步骤 33 仅提到"README.md"但未明确"version.md 详细版本说明"。
- **遗漏2**：步骤 18 提到"如无此表，需新增 migration 创建"player_sessions 表，但未将"daemon 写入链路"列为独立步骤。表创建后若无 daemon 写入逻辑，API 查询将永远返回空数据，构成假闭合风险（详见 F19）。

**建议**：
1. 步骤 33 拆分明确为："版本源统一升级 v4.13.0（package.json ×4 / version.json / deploy.sh / README.md / **version.md 详细版本说明** / daemon DAEMON_VERSION）"。
2. 在步骤 18 之前新增独立步骤："player_sessions 表 migration + daemon 玩家加入/离开事件写入逻辑"，并在闭合判据中加入"daemon 写入链路 grep 验证"。

---

#### A3. 是否有步骤间的逻辑冲突？

**判定：通过**

**证据**：
- 步骤 1（契约变更申请[V]）阻塞步骤 2（数据契约）→ 步骤 2 阻塞步骤 3-5（migration/Mock/配置）→ 步骤 3 阻塞步骤 6（AssetService）→ 步骤 6 阻塞步骤 7（前端 API）。链路清晰。
- 步骤 11（共享子组件抽离）先于步骤 8-10（三套视图），合理。
- 步骤 10（Guild 视图）依赖阶段一 banner_url 等字段，方案明确说明。
- 步骤 12-13（重定向）依赖步骤 8-10（三套视图），合理。
- 步骤 14（门控上提）依赖步骤 12-13，合理。
- 步骤 16（玩家操作）依赖步骤 15（玩家列表），合理。
- 步骤 20-21 依赖步骤 15-19，合理。
- 步骤 23-26（Player Portal）依赖步骤 2-7（契约扩展），合理。
- 步骤 27-32 验证依赖全部开发完成，合理。
- 步骤 33-36 版本升级，合理。
- 未发现明显逻辑冲突。

**建议**：无。

---

### B. 依赖 DAG 与并行策略（rules-0 §四-4）

#### B4. [P] 并行组标记是否合理？每组是否 ≤2（MAX_PARALLEL_PER_BATCH）？

**判定：通过**

**证据**：
- [P1] 步骤3 + 步骤4：2 个 ✅
- [P2] 步骤8 + 步骤9：2 个 ✅
- [P3a] 步骤15 + 步骤17：2 个 ✅
- [P3b] 步骤18 + 步骤19：2 个 ✅
- [P4a] 步骤23 + 步骤25：2 个 ✅
- [P4b] 步骤24 + 步骤26：2 个 ✅
- 全部 [P] 组均 ≤2，符合 MAX_PARALLEL_PER_BATCH=2。
- 每个 [P] 组都附并行理由（"独立聚合查询，无共享状态"等）。

**建议**：无。

---

#### B5. 全局并行是否 ≤3（MAX_PARALLEL_GLOBAL）？

**判定：警示**

**证据**：
- 批次1：[P1] = 2 并行 ✅
- **批次2：[P2] + [P3a] = 4 并行 ❌**（[P2] 是步骤8+9 共 2 个，[P3a] 是步骤15+17 共 2 个，合计 4 个并行任务，超过 MAX_PARALLEL_GLOBAL=3）。方案表格中标注"3（跨阶段）"，但实际任务数为 4，存在计算错误。
- 批次3：步骤10 + [P3b] = 3 并行 ✅
- 批次4：[P4a] + 步骤16 = 3 并行 ✅
- 批次5：[P4b] + 步骤20-21 = 3 并行 ✅（步骤20-21 串行算 1）

**建议**：
1. 批次2 调整为：先执行 [P2]（步骤8+9），待 [P2] 完成后再执行 [P3a]（步骤15+17）；或将 [P3a] 拆分一个到批次3。
2. 重新核算全局并行调度表，确保每批次实际并行任务数 ≤3。

---

#### B6. 依赖关系是否有遗漏（如步骤10是否真的依赖阶段一）？

**判定：警示**

**证据**：
- **步骤10 依赖关系描述不精确**：方案说"步骤10（Guild 视图）单独执行因依赖阶段一的 `banner_url` 等字段"。但实际上步骤10 的"构建依赖"只依赖步骤11（共享子组件），"功能完整依赖"才依赖阶段一的 banner_url 字段。即使契约字段未扩展，Guild 视图也可先实现（banner_url 为 null，向后兼容）。当前描述导致步骤10 被强行串行到阶段一之后，浪费并行机会。
- **步骤18 的 player_sessions 表创建应独立**：步骤18 提到"如无此表，需新增 migration 创建"，但表创建与 API 聚合查询是两个独立动作，且 daemon 写入链路是第三个独立动作（详见 F19）。当前步骤18 把三个动作混在一起，依赖关系不清晰。
- 步骤4（配置契约）与步骤3（migration）都依赖步骤2，可并行 ✅。
- 步骤16 依赖步骤15 ✅。

**建议**：
1. 步骤10 的依赖描述改为："构建依赖步骤11（共享子组件）；功能完整测试依赖阶段一 banner_url 字段。可在阶段一并行启动构建，完整 E2E 验证待阶段一完成后执行。"
2. 步骤18 拆分为三个子步骤：18a（migration 创建 player_sessions 表）、18b（daemon 玩家加入/离开事件写入逻辑）、18c（API 聚合查询）。

---

#### B7. 失败回退点是否明确？

**判定：通过**

**证据**：
- [P1] 失败回退：步骤2 完成检查点 ✅
- [P2] 失败回退：步骤11 完成检查点 ✅
- [P3a] 失败回退：阶段一完成检查点 ✅
- [P3b] 失败回退：阶段一完成检查点 ✅
- [P4a] 失败回退：阶段一完成检查点 ✅
- [P4b] 失败回退：步骤23 完成检查点 ✅
- 步骤16 失败回退：步骤15 完成检查点（台账中明确）✅
- 每个 [P] 组都有失败回退点，且回退到合理的检查点。

**建议**：无。

---

### C. 闭合判据（rules-0 §四-8 + §四-13）

#### C8. 每个 task 的闭合判据是否包含"运行时接入校验"？

**判定：警示**

**证据**：
- **阶段二（步骤8-14）**：通过 ✅
  - 步骤8-10：App.tsx 路由注册 grep ✅
  - 步骤11：前端 import grep ✅
  - 步骤12-13：路由表 grep ✅
  - 步骤14：App.tsx 门控 grep ✅
- **阶段三（步骤15-22）**：通过 ✅
  - 步骤15-19：`grep -rn "router.use\|app.use" panel/backend/src/api/index.ts` ✅
  - 步骤20：前端 api/client.ts 含四个新方法（grep）✅
  - 步骤21：前端 import grep（非 Mock）✅
  - 步骤22：requireRole 中间件 grep ✅
- **阶段一（步骤1-7）**：部分缺失 ⚠️
  - 步骤6 提到"services-init 注入不变"，但未给出具体 grep 命令。
  - 步骤2、5 是契约文件扩展，本身不需要运行时接入校验，但步骤6 的 AssetService 扩展应明确"AssetService 仍被 services-init.ts 注入"的 grep 验证命令（参考 v4.12.0 审计文档的 `grep -rn "AssetService" panel/backend/src/services/`）。
- **阶段四（步骤23-26）**：部分缺失 ⚠️
  - 闭合判据主要是 E2E 截图和 DOM 检查，没有明确"前端 fetch 真实 API"的校验。
  - 虽然步骤30 有"前端 fetch 真实 API"的 grep，但阶段四本身的闭合判据应包含此校验，避免阶段四单独闭合时漏检。

**建议**：
1. 阶段一步骤6 闭合判据补充："`grep -rn "AssetService" panel/backend/src/services/services-init.ts` 确认注入链路未破坏"。
2. 阶段四步骤23-26 闭合判据补充："前端 Guild 视图 fetch 真实 `/api/store/servers/:id/assets` API（grep `from.*api/client` 排除 Mock）"。

---

#### C9. 是否存在"文件存在+单测通过"但未接入运行时的孤岛风险（v4.11.0 假闭合教训）？

**判定：警示**

**证据**：
- **步骤15-19 的 services 注入描述模糊** ⚠️：闭合判据要求"四个路由文件存在 + services 注入"，但方案未明确这些 API 需要哪些 services。
  - 步骤16 明确"复用现有 `commandDispatcher` + `executionEngineClient`"✅
  - 步骤15、17、18、19 未明确是否需要 services 注入，还是直接操作数据库（Knex）。
  - 若这些 API 直接操作数据库，则"services 注入"校验不适用，应改为"路由文件 import 数据库连接"的 grep。
- **步骤23-26 的前端接入校验缺失** ⚠️：同 C8，未明确"前端 fetch 真实 API"。
- 步骤21 明确"非 Mock"，降低孤岛风险 ✅。
- 步骤30 明确"运行时接入 grep 全通过" ✅。

**建议**：
1. 步骤15、17、18、19 明确数据访问层：若直接操作数据库，闭合判据改为"路由文件 import `db` 或 `knex` 实例"的 grep；若需 services，明确具体 services 名称。
2. 步骤23-26 闭合判据补充前端 fetch 真实 API 的 grep（同 C8 建议）。

---

#### C10. grep 验证命令是否可执行且覆盖关键接入点？

**判定：警示**

**证据**：
- 步骤15-19 的 grep：`grep -rn "router.use\|app.use" panel/backend/src/api/index.ts`
  - 此命令验证路由聚合器是否挂载新路由，但无法确认具体路由文件（store-players.ts / store-reports.ts / store-servers.ts）是否被 import。
  - 应补充：`grep -rn "store-players\|store-reports\|store-servers" panel/backend/src/api/` 确认具体路由文件被引用。
- 步骤30 的 grep：
  - "运行时接入 grep 全通过（路由注册 + services 注入 + 前端 fetch 真实 API）"
  - 未给出具体的 grep 命令清单，仅描述校验项。
  - 应给出可执行的具体命令，参考 v4.12.0 审计文档的 grep 清单格式。

**建议**：
1. 步骤15-19 闭合判据补充精确 grep：
   ```bash
   grep -rn "store-players\|store-reports\|store-servers" panel/backend/src/api/index.ts
   ```
2. 步骤30 补充具体 grep 命令清单：
   ```bash
   # 路由注册
   grep -rn "router.use\|app.use" panel/backend/src/api/index.ts
   grep -rn "store-players\|store-reports\|store-servers" panel/backend/src/api/
   # services 注入（若适用）
   grep -rn "AssetService" panel/backend/src/services/services-init.ts
   # 前端 fetch 真实 API（非 Mock）
   grep -rn "from.*api/client" panel/frontend/src/pages/admin/ServerDetailAdmin.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/store/ServerDetailStore.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/guild/ServerDetailGuild.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/store/StorePlayers.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/store/StoreReportsRevenue.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/store/StoreReportsPlaytime.tsx
   grep -rn "from.*api/client" panel/frontend/src/pages/store/StoreServers.tsx
   # daemon 写入链路（若步骤18 拆分）
   grep -rn "player_sessions" daemon/src/
   ```

---

### D. subagent 调度台账（rules-0 §四-11）

#### D11. 台账是否含全部必填字段？

**判定：通过**

**证据**：
- 台账表头包含全部 8 个必填字段：阶段标签 / [P]组 / subagent_type / 预期产物 / actual agent id / 第二落点 / 失败回退点 / 状态 ✅
- 每一行都有完整字段填写，无空置。
- actual agent id 字段统一标注"待回填（启动后填入真实拉起ID）"或"主线程（非subagent）"，符合 rules-0 §四-11 规范。
- 台账末尾附规则说明："actual agent id 在 subagent 启动后回填真实任务标识；主线程内联执行填'主线程（非subagent）'；因不可抗力缺失须注明原因。同阶段重开时保留前驱 ID 作为 `previous_id`，追加 `retry_count` 和 `failure_reason`。" ✅

**建议**：无。

---

#### D12. subagent_type 是否统一为 general_purpose_task？

**判定：通过**

**证据**：
- 所有 subagent 行的 subagent_type 均为 `general_purpose_task` ✅
- 主线程内联执行的行标注"主线程（非subagent）"，未填 subagent_type，符合规范（主线程不涉及 subagent 调度）。
- GN-004 方案审查行也标注 `general_purpose_task` ✅

**建议**：无。

---

#### D13. 第二落点路径是否具体？

**判定：通过**

**证据**：
- 全部 18 行台账的第二落点路径均具体到文件或目录级别 ✅
- 示例：
  - 步骤1-2：`public/schema/panel-api-types.ts`
  - 步骤3-4：`panel/backend/src/db/migrations/`
  - 步骤8-9：`pages/admin/ServerDetailAdmin.tsx` / `pages/store/ServerDetailStore.tsx`
  - 步骤15,17：`panel/backend/src/api/routes/store-players.ts` / `store-reports.ts`
  - GN-004：`.trae/documents/20260724_v4.13.0_GN004方案审查.md`

**建议**：无。

---

### E. public/ 契约保护（rules-0 §四-10 + §四-7.2）

#### E14. Instance_Assets 契约扩展是否明确标注需走 s0601 流程 + 人类显式授权？

**判定：通过**

**证据**：
- 步骤1："按 s0601 适配契约变更流程，向人类提交 `instance_assets` 表扩展申请，包含字段清单、类型、默认值、影响范围" ✅
- 范围边界："Instance_Assets 契约扩展必须走 s0601 流程并经人类显式授权，不得由 LLM 自行编辑 `public/schema/` 下的任何文件" ✅
- 风险与建议："契约扩展需授权：`public/schema/panel-api-types.ts` 属受保护路径，扩展前必须通过 `ec7_action_gate` 拦截并获人类授权" ✅
- 三处明确标注，覆盖申请、执行、风险三个层面。

**建议**：无。

---

#### E15. 是否存在 LLM 自行编辑 public/ 的风险路径？

**判定：警示**

**证据**：
- 步骤2 涉及 `public/schema/panel-api-types.ts`（受保护路径），步骤5 涉及 `public/pre_generated_mock/`（受保护路径）。
- 方案在范围边界和风险中明确禁止 LLM 自行编辑，但步骤2、5 本身的执行动作描述为"在 `public/schema/panel-api-types.ts` 中新增字段"和"更新 `public/pre_generated_mock/` 下的 Mock 实现"。
- 台账显示步骤1-2、步骤5 的执行主体是"主线程（非subagent）"。
- **风险点**：虽然方案声明禁止 LLM 自行编辑，但步骤2、5 的动作描述未显式标注"执行前必须经 `ec7_action_gate` 拦截并获人类授权"。主线程在执行时可能因上下文遗忘而直接调用 Write/Edit 工具修改 public/ 文件，绕过 ec7_action_gate。

**建议**：
1. 步骤2 动作描述补充："**执行前必须经 `ec7_action_gate` 拦截，获人类显式授权后方可编辑 `public/schema/panel-api-types.ts`**"。
2. 步骤5 动作描述补充："**执行前必须经 `ec7_action_gate` 拦截，获人类显式授权后方可编辑 `public/pre_generated_mock/` 下文件**"。
3. 步骤4（配置契约更新）同理，若涉及 `public/config_template/` 也需补充 ec7_action_gate 标注。

---

#### E16. 新字段是否均为可选（默认 null）保证向后兼容？

**判定：通过**

**证据**：
- 步骤2 字段定义：
  - `banner_url?: string` — 可选 ✅
  - `banner_link?: string` — 可选 ✅
  - `shop_description?: string` — 可选 ✅
  - `shop_theme_color?: string` — 可选 ✅
- 步骤3 migration："均可空，默认 null" ✅
- 范围边界："新字段均为可选（默认 null），保证向后兼容" ✅
- TypeScript `?:` 语法 + migration 默认 null，双重保证向后兼容。

**建议**：无。

---

### F. 假闭合风险（v4.11.0 教训）

#### F17. GM Workbench 四个后端 API 是否有"仅创建路由文件但未接入 services"的风险？

**判定：警示**

**证据**：
- 步骤15-19 的闭合判据要求"四个路由文件存在 + services 注入"，但：
  - 步骤16 明确"复用现有 `commandDispatcher` + `executionEngineClient`" ✅（services 注入清晰）
  - 步骤15（玩家列表）、步骤17（流水报表）、步骤18（时长统计）、步骤19（服主实例列表）未明确是否需要 services 注入。
  - 若这些 API 直接操作数据库（Knex），则"services 注入"校验不适用，应改为"路由文件 import 数据库连接"的 grep。
  - 若这些 API 需要 services（如 PlayerService、OrderService），方案未明确具体 services 名称。
- 步骤21 明确"非 Mock"，降低前端孤岛风险 ✅。
- 步骤30 明确"运行时接入 grep 全通过" ✅。

**建议**：
1. 步骤15、17、18、19 明确数据访问层：
   - 若直接操作数据库：闭合判据改为"路由文件 import `db` 或 `knex` 实例的 grep"。
   - 若需 services：明确具体 services 名称（如 PlayerService、OrderService）和注入路径。
2. 避免"仅创建路由文件但未接入 services"的假闭合，参考 v4.11.0 教训。

---

#### F18. 前端占位页替换是否明确要求"fetch 真实 API 而非 Mock"？

**判定：通过**

**证据**：
- 步骤21 闭合判据："四个占位页替换为真实数据页面 + tsc 通过"，"前端 import grep（非 Mock）" ✅
- 步骤30 闭合判据："运行时接入 grep 全通过（路由注册 + services 注入 + 前端 fetch 真实 API）" ✅
- 两处明确要求"非 Mock"和"fetch 真实 API"。

**建议**：无。

---

#### F19. 步骤18 时长统计若需新建 player_sessions 表，daemon 写入链路是否覆盖？

**判定：警示**

**证据**：
- 步骤18："从 `player_sessions` 表聚合（如无此表，需新增 migration 创建）"
- 风险与建议："玩家时长统计依赖 `player_sessions` 表：如该表不存在，需新增 migration 创建，并确保 daemon 在玩家加入/离开时写入数据"
- **关键遗漏**：方案提到"需确保 daemon 在玩家加入/离开时写入数据"，但未将 daemon 写入逻辑列为独立步骤，也未在闭合判据中加入 daemon 写入链路的 grep 验证。
- **假闭合风险**：若仅创建表和 API，但 daemon 不写入数据，API 查询将永远返回空数据。这是典型的"文件存在 + 单测通过 + build 通过"但运行时无数据的孤岛场景，违反 rules-0 §四-13。
- 步骤30 的运行时接入 grep 未包含 daemon 写入链路验证。

**建议**：
1. 步骤18 拆分为三个子步骤：
   - 18a：migration 创建 `player_sessions` 表（schema：session_id / player_user_id / instance_id / join_at / leave_at / duration_seconds）。
   - 18b：daemon 玩家加入/离开事件写入逻辑（订阅现有事件总线或 RCON 事件，写入 player_sessions 表）。
   - 18c：API 聚合查询（从 player_sessions 表按日聚合）。
2. 闭合判据补充：
   - 18a：migration 文件存在 + `knex migrate:latest` 通过。
   - 18b：`grep -rn "player_sessions" daemon/src/` 确认 daemon 写入链路存在。
   - 18c：API 路由文件存在 + 路由聚合器挂载 grep。
3. 步骤30 运行时接入 grep 补充：`grep -rn "player_sessions" daemon/src/`。

---

### G. 安全与门控

#### G20. 三基座路由门控是否正确？

**判定：通过**

**证据**：
- 步骤14：
  - `/admin/servers/*` → `server_admin+` ✅
  - `/store/servers/*` → `instance_admin+` ✅
  - `/guild/servers/*` → `user+` ✅
- 与 v4.12.0 角色门控审计文档（`.trae/documents/20260724_v4.12.0_角色门控审计.md`）的三基座门控一致：
  - `/admin` (server_admin+) ✅
  - `/store` (instance_admin+) ✅
  - `/guild` (user+) ✅
- 门控上提到基座 Layout 层，子路由不再重复门控（除细粒度保留项）。

**建议**：无。

---

#### G21. /api/store/* 后端端点门控是否 instance_admin+？

**判定：通过**

**证据**：
- 步骤22："所有 `/api/store/*` 端点门控 `instance_admin+`，通过 `requireRole` 中间件校验" ✅
- 与前端 `/store/servers/*` 门控 `instance_admin+` 对齐，前后端门控一致。

**建议**：无。

---

#### G22. 旧路径重定向是否按角色分流，而非统一跳转？

**判定：通过**

**证据**：
- 步骤12：
  - `/instances/:id` → 按角色重定向（`server_admin → /admin/servers/:id`、`instance_admin → /store/servers/:id`、`user → /guild/servers/:id`）✅
- 步骤13：
  - `/instances` → 按角色重定向（`server_admin → /admin/servers`、`instance_admin → /store/servers`、`user → /guild`）✅
  - `/instances/new` → `/store/servers/new`（仅服主可创建）✅
- 重定向逻辑按角色分流，与 v4.12.0 的 RootRedirect 角色跳转策略一致。
- 子路径重定向（shop → guild、business → store、player-histories → admin）按功能归属分流 ✅。

**建议**：无。

---

### H. 风险评估充分性

#### H23. 方案列出的风险是否全面？是否有未识别的风险？

**判定：警示**

**证据**：
- 已列风险（5 项）：
  1. 实例详情页拆分影响面大（738KB bundle）✅
  2. 玩家时长统计依赖 player_sessions 表 ✅
  3. 服主店铺主题色动态应用 ✅
  4. 契约扩展需授权 ✅
  5. 旧路径重定向保留期 ✅
- **未识别风险（6 项）**：
  1. **数据库迁移风险**：player_sessions 表创建可能影响现有数据，应备份现有 instance_assets 表后再执行 migration（v4.12.0 重构前有 backup/v4.12.0-pre-refactor-20260724/ 备份，v4.13.0 应同样备份）。
  2. **daemon 写入链路风险**：daemon 新增 player_sessions 写入逻辑可能与现有玩家事件处理逻辑冲突，需回归测试现有玩家加入/离开流程。
  3. **前端 bundle 拆分风险**：三套视图拆分后，共享子组件抽离不当可能导致 bundle 反而变大（如循环依赖、重复打包）。
  4. **移动端视口守卫风险**（rules-0 §3.1.6）：Player Portal 的 Banner 占顶部 ≥30% 视口高度，若使用 `height: 30vh` 强制可能导致移动端地址栏显示时实际高度不足。应使用 `min-height: 30vh` 或 `height: 30dvh`。
  5. **React Router v6 路由结构守卫风险**（rules-0 §3.1.1）：三套视图拆分涉及大量路由重组，可能违反 `<Routes>` 直接子元素只能是 `<Route>` 或 `<React.Fragment>` 的规则。需在步骤8-10 的闭合判据中加入路由结构验证。
  6. **角色门控审计文档更新风险**：v4.13.0 完成后应更新 `.trae/documents/20260724_v4.12.0_角色门控审计.md`（或新建 v4.13.0 审计文档），记录 `/instances/*` 拆分后的门控清单。方案未列入此步骤。

**建议**：
1. 风险清单补充上述 6 项未识别风险。
2. 步骤33-36 版本升级前新增步骤："备份当前生产环境到 `backup/v4.13.0-pre-refactor-{date}/`"（参考 v4.12.0 备份实践）。
3. 步骤27-32 验证阶段新增："更新角色门控审计文档，记录 `/admin/servers/*`、`/store/servers/*`、`/guild/servers/*` 门控清单"。

---

#### H24. ServerDetail 拆分（738KB bundle）的风险控制是否充分？

**判定：警示**

**证据**：
- 风险与建议提到："建议先抽离共享子组件，再分别实现三套视图，每步构建验证" ✅
- 步骤11（共享子组件抽离）先于步骤8-10（三套视图）✅
- **缺失1**：方案提到"每步构建验证"，但步骤8-10 的闭合判据仅有"tsc 通过"，未包含 bundle size 检查。
- **缺失2**：未设定 bundle size 优化目标。738KB 拆分后应设定每个视图 bundle 目标（如 < 300KB），否则无法验证拆分效果。
- **缺失3**：未考虑代码分割（React.lazy + Suspense）策略，三套视图应按路由懒加载，避免整体打包。

**建议**：
1. 步骤8-10 闭合判据补充："构建验证（`npm run build` 通过 + bundle size 报告检查）"。
2. 设定 bundle size 目标：三套视图各自 bundle < 300KB（共享子组件抽离后）。
3. 步骤11 共享子组件抽离时，明确使用 `React.lazy` + `Suspense` 懒加载策略，确保三套视图按路由分割。
4. 步骤31 前端三重测试闸门补充："build 产物 bundle size 分析报告"作为验证产物。

---

## 三、总结论

### 总判定：警示放行

- **通过项**：13 项（A1, A3, B4, B7, D11, D12, D13, E14, E16, F18, G20, G21, G22）
- **警示项**：11 项（A2, B5, B6, C8, C9, C10, E15, F17, F19, H23, H24）
- **阻断项**：0 项

### 警示项必须补强的内容

> 主线程在 NotifyUser 通知人类前，须将以下 11 项警示补强到方案文档，或显式知悉并记录到 note。

| 编号 | 警示项 | 必须补强的内容 |
|------|--------|---------------|
| A2 | 36 步遗漏 | 1) 步骤33 明确"version.md 详细版本说明"；2) 新增步骤"player_sessions 表 migration + daemon 写入逻辑"（与 F19 联动） |
| B5 | 全局并行超限 | 批次2 [P2]+[P3a] 实际 4 并行，超过 MAX_PARALLEL_GLOBAL=3。调整批次2 为先 [P2] 后 [P3a]，或将 [P3a] 拆分一个到批次3 |
| B6 | 依赖关系不精确 | 1) 步骤10 依赖描述区分"构建依赖"和"功能完整依赖"；2) 步骤18 拆分为 18a/18b/18c 三子步骤 |
| C8 | 闭合判据缺失运行时接入 | 1) 阶段一步骤6 补充 AssetService 注入 grep；2) 阶段四步骤23-26 补充前端 fetch 真实 API grep |
| C9 | services 注入描述模糊 | 步骤15、17、18、19 明确数据访问层（直接操作数据库 or services 注入），并对应调整闭合判据 |
| C10 | grep 命令不精确 | 1) 步骤15-19 补充精确 grep（store-players/store-reports/store-servers）；2) 步骤30 给出具体 grep 命令清单 |
| E15 | public/ 编辑未标注 ec7_action_gate | 步骤2、4、5 动作描述补充"执行前必须经 ec7_action_gate 拦截并获人类显式授权" |
| F17 | API services 注入风险 | 同 C9，步骤15、17、18、19 明确 services 注入或数据库直接访问 |
| F19 | daemon 写入链路遗漏 | 步骤18 拆分为 18a（migration）/18b（daemon 写入）/18c（API 聚合），闭合判据补充 daemon 写入链路 grep |
| H23 | 风险评估不全面 | 1) 补充 6 项未识别风险；2) 新增步骤"备份生产环境"；3) 新增步骤"更新角色门控审计文档" |
| H24 | bundle 拆分风险控制不足 | 1) 步骤8-10 闭合判据补充 bundle size 检查；2) 设定 bundle size 目标 < 300KB；3) 明确 React.lazy 懒加载策略 |

### 阻断项

无。

### 审查者声明

1. 本审查为 GN-004 独立审查能力承载者（由 general_purpose_task 承载）执行，仅审查、不写任何代码，未修改任何源码文件。
2. 本审查基于方案文档、current-note.md、v4.12.0 角色门控审计、v4.12.0 重构方案、rules-0 进行，审查上下文完整。
3. 本审查为 subagent 上下文执行。若主线程需对警示项作出补强后复审，须重新拉起 GN-004 完整审查（不得仅审"上次那一点"）。
4. 主线程在合流前须复核本审查结论，对警示项作出补强或显式知悉并记录到 note，方可 NotifyUser 通知人类。
