# Test2 — 内置浏览器核对
# 执行时间: 2026-07-30 12:58
# 状态: 未闭合（用户决定暂缓部署，Test2 暂不执行）

## 原因
本次 info-card 布局改动需新构建生效后才能在浏览器核对。用户在价值判断节点选择"先不部署"，
故 Test2 内置浏览器核对暂缓。

## 待核对场景（部署后执行）
1. 导航到 https://gsp.ecsrz.com:3001/instances/{id}（实例详情页）
2. 展开信息卡，确认：
   - 上半 `.info-card-2col` 2 列网格只含 9 条短行，横线对齐
   - 下半 `.info-card-ops` 单列含 6 条交互行，与上半之间有 1px 分隔线
   - "续费" 行：select + 立即续费按钮 在全宽内不挤压
   - "子目录清理" 行：5 个按钮在全宽内排布，不再多行折行错位
   - label 与 value 垂直居中对齐
3. 截图取证，记录到本文件

## 预期视觉效果
- 短展示行紧凑 2 列，无大片留白
- 交互行独占全宽，控件不再被半列挤压
- 两区间有清晰分隔线，语义分区明确

## next_actions
- 用户决定部署时：bump 4.37.1→4.37.2（12 处版本源）→ s0701 预检 → 关闭现有部署 → deploy.sh update → 重跑 Test2

## rerun_entry
部署完成后，重跑本 Test2：导航实例详情页 → 截图 → 回填本文件
