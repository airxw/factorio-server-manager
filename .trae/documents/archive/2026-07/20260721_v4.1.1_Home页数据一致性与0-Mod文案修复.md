# v4.1.1 Home 页数据一致性与 0-Mod 文案修复

## 一、变更范围

- 公开路由 `/home` 落地页（仅前端，不涉及后端 / daemon / 数据库）
- 范围：消除 4 套数据多源 + 修正文案矛盾 + 修 hooks 状态机 + clipboard 降级
- 触发：用户反馈"home 页面有问题"，深度逻辑分析后发现 4 套数据多源 + LuckPerms 矛盾

## 二、工程过程

| # | 任务 | 文件 | 状态 |
|---|------|------|------|
| 1 | 修复 Home.tsx 不存在的 `PackInfo` / `PACKS` 引用 | `src/pages/Home.tsx` | 已完成 |
| 2 | 补充 `GAME_LANDING_DATA` import | `src/pages/Home.tsx` | 已完成 |
| 3 | PackCloud 改用 `GAME_CATALOG`（消除硬编码 `PACKS` 数组） | `src/pages/Home.tsx` | 已完成 |
| 4 | PackCloud 字段对齐：tag→tagline, port→portDisplay, rcon→rconDisplay | `src/pages/Home.tsx` | 已完成 |
| 5 | Footer 改用 `useAppVersion`，去除 getVersion 双请求 | `src/pages/Home.tsx` | 已完成 |
| 6 | DeployCTA GitHub 链接 `https://github.com` → `SITE_CONFIG.repoUrl` | `src/pages/Home.tsx` | 已完成 |
| 7 | copyCommand clipboard 不可用时降级为 `execCommand` 兜底 + 失败提示 | `src/pages/Home.tsx` | 已完成 |
| 8 | 删除未用的 `Suspense` import | `src/pages/Home.tsx` | 已完成 |
| 9 | 删除 `useScrollReveal([theme])` 多余参数（函数签名 0 参） | `src/pages/Home.tsx` | 已完成 |
| 10 | 删除 `game-landing-data.ts` 未用的 `Brain` import | `src/data/game-landing-data.ts` | 已完成 |
| 11 | TS 编译验证 | — | ✅ 通过 |
| 12 | `npm run build` 验证 + localhost:3000 检查 | — | ✅ 通过 |
| 13 | 版本号自增 4.1.0 → 4.1.1（小版本号 +1，符合"bug 修复"语义） | `version.json` / 4 个 `package.json` / `deploy.sh` / `README.md` / `version.md` | 已完成 |

## 三、交接状态

- **当前 task**：v4.1.1 全部 task 已完成
- **状态**：已闭合
- **变更类型**：bug 修复（小版本号 +1）
- **数据迁移**：无（仅前端文案与数据源重构）
- **回退锚点**：v4.1.0 commit（`version.json` `4.1.0` + `panel/frontend/src/pages/Home.tsx` 旧版本）

## 四、最终结果

### 已完成

- **数据多源 → 单一来源**：`PackCloud` 不再读硬编码 `PACKS` 数组，消费 `GAME_CATALOG`（12 款游戏的完整字段）
- **0-Mod 文案一致**：`TERMINAL_SCRIPT` 第 8 行由 `/lp user Steve parent set gold` 改为 `gsp vip grant @Steve gold`；Hero 明确"运营能力 0 游戏内 Mod 依赖"的范围限定为 VIP/商城/CDK/礼包/投票 五项
- **能力数字统一**：5 项运营能力（`OPERATION_CAPS.length`）与 5 痛点 / 5 解决方案一一对应
- **Footer 双请求去重**：`AppVersionContext` 共享 `getVersion` 结果
- **clipboard 失败降级**：`execCommand('copy')` 兜底 + 失败提示
- **Hooks 状态机修复**：`useScrollReveal` / `useActiveSection` / `useTypewriter` 避免首屏空白 / 闪烁 / 重渲
- **占位符清理**：`DEPLOY_COMMAND` 与 GitHub 链接全部使用 `SITE_CONFIG.repoUrl`
- **TS 编译 + 构建 + 验证**：全部通过

### 验证证据链

```
$ npx tsc --noEmit
$ npm run build
✓ built in 7.41s
✅ Build verification passed  (无 localhost:3000/127.0.0.1:3000)
```

### 产出物清单

- 新增：
  - `panel/frontend/src/data/game-catalog.ts`（12 款游戏完整字段）
  - `panel/frontend/src/data/game-landing-data.ts`（9 款游戏落地页数据）
  - `panel/frontend/src/context/AppVersionContext.tsx`（版本共享 Context）
- 修改：
  - `panel/frontend/src/pages/Home.tsx`（1927 行，含全部修复）
  - `panel/frontend/src/App.tsx`（注入 `AppVersionProvider`）
  - `version.json` / 4 个 `package.json` / `deploy.sh` / `README.md` / `version.md`（4.1.0 → 4.1.1）

## 五、注意事项

- 部署前需执行 `npm run build` 重新生成 `dist/`
- 前端 dist 部署路径必须为 `/opt/gameserver-panel/panel/frontend/dist/`（不是 `/opt/gameserver-panel/dist/`）
- 生产部署前需关闭现有服务（与 `deploy.sh` 逻辑一致）
