# v4.1.0 7 个新游戏 Pack 完整运行支持

> **变更类型**：全新功能（中版本号 +1）
> **变更日期**：2026-07-20
> **影响范围**：daemon / panel/backend / packs/ / version / 文档

---

## 一、工程过程（已完成 task）

| # | Task | 产出物 | 状态 |
|---|------|--------|------|
| 1 | 调研 7 个游戏（dst / terraria / valheim / zomboid / enshrouded / satisfactory / dyson）的专用服务器资料 | `游戏专用服务器调研报告.md` + `游戏专用服务器调研报告2-Valheim-Zomboid.md` | 已完成 |
| 2 | 制定方案 MD 文档 | `docs/v4.1.0-7-games-pack-completion-plan.md` | 已完成（用户审批） |
| 3 | daemon 代码扩展：STEAM_APP_IDS / bootstrap switch / 辅助函数 / manager vars / SIGINT 停止策略 | `daemon/src/steamcmd/apps.ts` + `daemon/src/instances/bootstrap.ts` + `daemon/src/instances/manager.ts` + `daemon/src/instances/processDriver.ts` | 已完成 |
| 4 | panel/backend 静态版本数据源扩展 7 个新 pack | `panel/backend/src/core/packs/staticVersions.ts` | 已完成 |
| 5 | 修正 7 个 pack.yaml 实战参数 | `packs/{dst,terraria,valheim,zomboid,enshrouded,satisfactory,dyson}-vanilla/pack.yaml` | 已完成 |
| 6 | 本地构建验证 | daemon / panel/backend / panel/frontend 三个项目 `tsc --noEmit` 全部通过；frontend `vite build` 成功；7 个 pack.yaml 通过 zod schema 校验 | 已完成 |
| 7 | 部署到生产服务器 | sync 到 `/opt/gameserver-panel/`；重启 systemd 服务；panel 日志确认 `count:12` pack 加载成功；daemon WS 连接正常 | 已完成 |
| 8 | 版本号与文档更新 | `version.json` / `version.md` / 4 个 `package.json` / `deploy.sh` / `README.md` 同步至 4.1.0 | 已完成 |

## 二、交接状态

- **当前 task**：v4.1.0 全部 task 已完成，本变更记录已归档
- **下一 task**：无（交付完成）
- **未闭合项**：无

## 三、最终结果

### 产出物清单

1. 调研报告：`游戏专用服务器调研报告.md` + `游戏专用服务器调研报告2-Valheim-Zomboid.md`
2. 方案文档：`docs/v4.1.0-7-games-pack-completion-plan.md`
3. 代码变更：
   - `daemon/src/steamcmd/apps.ts` — STEAM_APP_IDS 新增 6 个映射
   - `daemon/src/instances/bootstrap.ts` — switch 新增 7 个 case + 4 个辅助函数
   - `daemon/src/instances/manager.ts` — vars 新增 17 个变量 + SIGINT 停止策略
   - `daemon/src/instances/processDriver.ts` — stop 方法新增 firstSignal 参数
   - `panel/backend/src/core/packs/staticVersions.ts` — 新增 7 个 STATIC_VERSION_SOURCES
4. Pack 配置变更：`packs/{dst,terraria,valheim,zomboid,enshrouded,satisfactory,dyson}-vanilla/pack.yaml` 7 个文件
5. 版本元数据：`version.json` / `version.md` / 4 个 `package.json` / `deploy.sh` 同步至 4.1.0
6. 用户文档：`README.md` 同步至 4.1.0，pack 列表从 5 个扩展到 12 个

### 验证结论

- `/admin/packs` 页面 12 个 pack 全部加载成功（生产日志确认 `count:12`）
- 现有 5 个 pack 不受影响
- 6 个游戏（dst / terraria / valheim / zomboid / enshrouded / satisfactory）支持完整 SteamCMD 自动下载 + 自动启动
- 1 个游戏（dyson）保留占位模式，用户需手动放置游戏目录 + BepInEx + Nebula Mod
- 不涉及数据库迁移；不修改 `public/schema/pack-schema.ts`（v4.0.4 已扩展 13 个 game type）
- 不修改公共契约，符合 AC 范式 rules-0 §四-10 public/ 保护规则

### 用户使用须知

- DST 首次启动需用户到 https://accounts.klei.com/account/serverTokens 生成 token 并替换 `cluster_token.txt`
- Satisfactory 首次启动需用户通过游戏内 Server Manager 设置管理员密码（Claim 流程）
- Enshrouded 需要 Wine 运行环境（binary=`wine`，args=`['./enshrouded_server.exe']`）
- Dyson 需要用户手动放置游戏目录 + 安装 BepInEx + Nebula Mod（不支持 SteamCMD anonymous 下载）
