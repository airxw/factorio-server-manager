这是一个linux 项目，不要考虑任何windows 相关的问题，
这是一个前端加后端的项目，注意前后分离
部署的时候，要关闭现有部署，否则会报错。
版本更新时要注意数据库字段，字段的变更，需要做迁移脚本的前置处理，方便热更新。
8080端口是对公网（外网）禁用端口，不能从外部访问。
服务器的root密码是xwxiewei
页面底部规范化 footer span 显示唯一“BUILD”序列（格式：`© YYYY GSP · Game Server Panel · BUILD YYYYMMDD-XXX`），用于确定部署结果是否符合预期；禁止在页面顶端、侧边栏等其他位置冗余渲染 BUILD 编号
网页/前端页面核对统一使用内置浏览器工具（`browser_*` / integrated browser）执行导航、交互、截图、快照与网络核验；禁止使用 Playwright 作为页面验证、E2E 页面核对或视觉确认入口。
这是科技生产力的面板，看齐苹果的清新设计语言，严禁使用杀马特电竞风和配色。主体颜色不建议使用深色配色。
