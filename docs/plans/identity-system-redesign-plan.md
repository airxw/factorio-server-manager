---
type: plan
title: 身份体系重构方案
date: 2026-07-25
status: deployed
deployed_in: v4.16.x
related: [login, identity, auth]
tags: [身份体系, 登录流程, 多角色切换]
---

# 身份体系重构方案

## 一、背景与目标

### 1.1 当前问题

- 登录页通过 URL `from` 参数推断用户身份上下文，逻辑隐式且易出错
- 演示账号暴露了系统管理员角色，违反权限隔离原则
- 用户可能同时拥有多个身份（既是腐竹又是玩家），但缺乏切换机制
- 新注册用户无身份引导，不知道如何选择入口

### 1.2 设计目标

1. **登录与身份解耦**：登录页只做认证（验证"你是谁"），不做身份选择
2. **身份显式化**：用户登录后显式选择/切换身份，系统记录默认身份
3. **多身份支持**：一个用户可拥有多个身份（腐竹/玩家），可随时切换
4. **系统管理员隔离**：系统管理员为独立账号，不参与身份选择流程，腐竹/玩家不可见

---

## 二、核心概念定义

### 2.1 身份类型

| 身份类型 | 标识 | 说明 | 获取方式 |
|---------|------|------|---------|
| 腐竹（实例管理员） | `instance_admin` | 管理自己的游戏服务器 | 用户主动选择 + 创建实例后自动绑定 |
| 玩家（普通用户） | `player` | 加入别人的游戏服务器 | 用户主动选择 + 绑定实例后自动绑定 |
| 系统管理员 | `server_admin` | 管理整个平台 | 初始化向导创建，独立账号 |

### 2.2 身份状态

- **无身份**：新注册用户，身份列表为空
- **单身份**：用户只有一个身份，登录后直接进入对应工作台
- **多身份**：用户有多个身份，登录后弹出身份选择界面

---

## 三、用户流程设计

### 3.1 新注册用户流程

```
注册 → 登录 → 检测到无身份 → 强制身份选择页
                    ↓
              选择"我是腐竹"或"我是玩家"
                    ↓
              身份绑定到账号 → 进入对应工作台
```

**身份选择页内容**：
- 标题："选择您的身份"
- 两个选项卡片：
  - **我是腐竹**：管理自己的游戏服务器，创建/配置/监控实例
  - **我是玩家**：加入别人的游戏服务器，购买道具/参与社区
- 选择后调用 API 绑定身份，跳转到对应工作台

### 3.2 已有身份用户流程

```
登录 → 检测到有身份 → 判断身份数量
                    ↓
              单身份 → 直接进入对应工作台
              多身份 → 弹出身份选择界面（列出所有身份）
                    ↓
              选择身份 → 进入对应工作台
```

### 3.3 身份切换流程

```
在工作台/门户中 → 点击个人头像 → 下拉菜单"切换身份"
                    ↓
              列出所有身份（腐竹/玩家）
                    ↓
              选择身份 → 页面切换到对应视图
                    ↓
              保持登录状态，不重新认证
```

### 3.4 系统管理员流程

```
系统管理员账号登录 → 直接进入管理后台（/admin）
                    ↓
              无身份选择，无切换入口
                    ↓
              腐竹/玩家看不到系统管理员存在
```

---

## 四、前端改造事项

### 4.1 登录页（Login.tsx）

**改造内容**：
1. 移除 `login-context-bar`（上下文标题条）
2. 移除演示账号区域（或改为可折叠，默认收起）
3. 登录成功后，调用 API 获取用户身份列表
4. 根据身份数量决定跳转：
   - 无身份 → 跳转到 `/select-identity`（身份选择页）
   - 单身份 → 直接跳转到对应工作台（腐竹→`/store`，玩家→`/guild`）
   - 多身份 → 跳转到 `/select-identity`（让用户选择本次进入哪个身份）

**文件**：`panel/frontend/src/pages/Login.tsx`

### 4.2 身份选择页（新增）

**新增页面**：`panel/frontend/src/pages/SelectIdentity.tsx`

**功能**：
- 展示用户所有身份列表（腐竹/玩家）
- 每个身份显示：
  - 身份类型图标
  - 身份名称（"腐竹" / "玩家"）
  - 关联的实例名称（如果有）
- 点击身份卡片，调用 API 设置当前活跃身份，跳转到对应工作台
- 如果是首次选择（无默认身份），选择后设置为默认身份

**路由**：`/select-identity`

### 4.3 个人头像下拉菜单改造

**改造内容**：
1. 在头像下拉菜单中增加"切换身份"选项
2. 点击后弹出身份列表 Modal 或 Dropdown
3. 选择身份后调用 API 切换，页面刷新为对应视图
4. 系统管理员账号不显示"切换身份"选项

**文件**：
- `panel/frontend/src/components/layout/Header.tsx`（或对应的头像组件）
- `panel/frontend/src/components/IdentitySwitcher.tsx`（新增身份切换组件）

### 4.4 路由守卫改造

**改造内容**：
1. `/store`（腐竹工作台）路由守卫：检查当前用户是否有腐竹身份
2. `/guild`（玩家门户）路由守卫：检查当前用户是否有玩家身份
3. 无权限时跳转到身份选择页或提示错误

**文件**：
- `panel/frontend/src/App.tsx`（路由配置）
- `panel/frontend/src/components/ProtectedRoute.tsx`（路由守卫组件）

### 4.5 全局状态管理

**改造内容**：
1. 在 `AuthContext` 中增加 `currentIdentity` 状态
2. 登录后从 API 获取用户身份列表，存储到 Context
3. 提供 `switchIdentity(identityId)` 方法，切换身份后更新 Context
4. 提供 `hasIdentity(role)` 方法，检查用户是否有某身份

**文件**：`panel/frontend/src/api/auth.tsx`

---

## 五、后端改造事项

### 5.1 用户身份表设计

**新增表**：`user_identities`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | integer | 主键 |
| `user_id` | integer | 用户 ID（外键） |
| `identity_type` | string | 身份类型：`instance_admin` / `player` |
| `instance_id` | integer | 关联实例 ID（可选，腐竹身份关联自己创建的实例） |
| `is_default` | boolean | 是否为默认身份 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

**唯一约束**：`(user_id, identity_type, instance_id)`

### 5.2 用户表扩展

**扩展字段**：`users` 表

| 字段 | 类型 | 说明 |
|------|------|------|
| `current_identity_id` | integer | 当前活跃身份 ID（外键到 `user_identities.id`） |

### 5.3 API 接口设计

#### 5.3.1 获取用户身份列表

**接口**：`GET /api/auth/identities`

**响应**：
```json
{
  "identities": [
    {
      "id": 1,
      "identity_type": "instance_admin",
      "instance_id": 10,
      "instance_name": "我的世界服务器",
      "is_default": true
    },
    {
      "id": 2,
      "identity_type": "player",
      "instance_id": 20,
      "instance_name": "帕鲁服务器",
      "is_default": false
    }
  ]
}
```

#### 5.3.2 创建身份

**接口**：`POST /api/auth/identities`

**请求**：
```json
{
  "identity_type": "instance_admin",
  "instance_id": 10
}
```

**响应**：
```json
{
  "id": 1,
  "identity_type": "instance_admin",
  "instance_id": 10,
  "is_default": true
}
```

#### 5.3.3 设置当前活跃身份

**接口**：`POST /api/auth/identities/:id/activate`

**响应**：
```json
{
  "success": true,
  "current_identity_id": 1
}
```

#### 5.3.4 设置默认身份

**接口**：`POST /api/auth/identities/:id/set-default`

**响应**：
```json
{
  "success": true
}
```

### 5.4 业务逻辑改造

#### 5.4.1 注册逻辑

**改造**：注册成功后，不自动创建身份，用户身份列表为空。

**文件**：`panel/backend/src/api/auth.ts`

#### 5.4.2 登录逻辑

**改造**：登录成功后，返回用户身份列表（或标记需要选择身份）。

**文件**：`panel/backend/src/api/auth.ts`

#### 5.4.3 创建实例逻辑

**改造**：腐竹创建实例后，自动创建 `instance_admin` 身份并绑定到该实例。

**文件**：`panel/backend/src/api/instances.ts`

#### 5.4.4 绑定实例逻辑

**改造**：玩家绑定实例后，自动创建 `player` 身份并绑定到该实例。

**文件**：`panel/backend/src/api/bindings.ts`

### 5.5 数据库迁移脚本

**新增迁移**：`panel/backend/migrations/YYYYMMDDHHMMSS_add_user_identities.js`

**内容**：
1. 创建 `user_identities` 表
2. 在 `users` 表增加 `current_identity_id` 字段
3. 迁移现有用户数据：
   - 已有实例的用户 → 创建 `instance_admin` 身份
   - 已有绑定的用户 → 创建 `player` 身份
   - 设置第一个身份为默认身份

---

## 六、系统管理员隔离方案

### 6.1 账号创建

**方式**：系统管理员账号通过初始化向导（`/setup`）或特殊 API 创建，不通过普通注册流程。

**标识**：`users.role = 'server_admin'`

### 6.2 登录流程

**改造**：系统管理员登录后，直接跳转到 `/admin`（管理后台），不经过身份选择页。

**文件**：`panel/frontend/src/pages/Login.tsx`

### 6.3 前端隔离

**改造**：
1. 腐竹工作台（`/store`）和玩家门户（`/guild`）不显示系统管理员相关信息
2. 系统管理员账号不显示"切换身份"入口
3. 系统管理员不能"变成"腐竹或玩家

**文件**：
- `panel/frontend/src/components/layout/Header.tsx`
- `panel/frontend/src/pages/Store.tsx`
- `panel/frontend/src/pages/Guild.tsx`

---

## 七、演示账号改造

### 7.1 当前演示账号

- `admin@local.dev`（服务器管理员）
- `manager@local.dev`（实例管理员）
- `user@local.dev`（普通用户）

### 7.2 改造方案

**方案 A（推荐）**：移除演示账号区域，登录页只保留邮箱+密码表单。

**方案 B**：保留演示账号区域，但改为可折叠（默认收起），点击"或使用演示账号"展开。

**文件**：`panel/frontend/src/pages/Login.tsx`

---

## 八、执行步骤

### 阶段 1：数据库与后端改造

1. 编写数据库迁移脚本，创建 `user_identities` 表
2. 在 `users` 表增加 `current_identity_id` 字段
3. 编写迁移逻辑，迁移现有用户数据
4. 实现身份相关 API（获取列表、创建、激活、设置默认）
5. 改造注册、登录、创建实例、绑定实例逻辑
6. 编写单元测试

### 阶段 2：前端改造

1. 改造登录页，移除上下文标题条，登录成功后根据身份数量决定跳转
2. 新增身份选择页（`SelectIdentity.tsx`）
3. 改造个人头像下拉菜单，增加"切换身份"入口
4. 改造路由守卫，检查身份权限
5. 改造全局状态管理，增加 `currentIdentity` 状态
6. 编写前端单元测试

### 阶段 3：系统管理员隔离

1. 确认系统管理员登录流程，直接跳转到 `/admin`
2. 移除腐竹/玩家视图中的系统管理员相关信息
3. 系统管理员账号不显示"切换身份"入口

### 阶段 4：演示账号改造

1. 决定采用方案 A（移除）或方案 B（可折叠）
2. 改造登录页演示账号区域

### 阶段 5：集成测试与部署

1. 端到端测试：注册→登录→身份选择→切换身份
2. 权限测试：腐竹访问玩家门户、玩家访问腐竹工作台
3. 系统管理员隔离测试
4. 部署到生产环境

---

## 九、建议与注意事项

### 9.1 数据迁移风险

- 现有用户可能没有身份数据，迁移脚本需要处理这种情况
- 建议迁移后为所有用户创建默认身份（根据现有实例/绑定关系）

### 9.2 前端状态同步

- `currentIdentity` 状态需要在多个组件间共享，建议使用 React Context
- 切换身份后需要刷新页面数据，避免缓存导致的数据不一致

### 9.3 权限校验

- 后端 API 需要校验用户是否有对应身份，防止越权访问
- 前端路由守卫需要检查身份，但后端 API 也需要二次校验

### 9.4 用户体验

- 身份选择页需要清晰展示每个身份的关联实例，避免用户混淆
- 切换身份后需要平滑过渡，避免页面闪烁

### 9.5 向后兼容

- 旧版本前端可能需要兼容旧版登录逻辑，建议后端 API 保持向后兼容
- 迁移脚本需要支持回滚，避免数据丢失

---

## 十、验收标准

1. 新注册用户登录后强制选择身份，选择后进入对应工作台
2. 已有身份用户登录后，单身份直接进入，多身份弹出选择界面
3. 用户可在个人位置切换身份，切换后页面刷新为对应视图
4. 系统管理员登录后直接进入管理后台，无身份选择，无切换入口
5. 腐竹/玩家看不到系统管理员相关信息
6. 演示账号区域可折叠或已移除
7. 端到端测试通过，权限校验通过
