---
type: plan
title: v4.19.1+ 推迟实施计划（v4.19.0 未完成内容承接）
date: 2026-07-25
status: v4.19.3-completed
deployed_in: v4.19.1-v4.19.3（M1/M3/M4/M6 已完成，M7/M8/M9 推迟至 v4.19.4+）
related:
  - docs/plans/v4.19.0-unified-release-plan.md
  - version.md
  - panel/backend/src/services/userService.ts
  - panel/frontend/src/pages/store/Players.tsx
  - panel/frontend/src/pages/Friends.tsx
  - panel/backend/src/services/inGameCommandService.ts
  - panel/backend/src/services/friendService.ts
  - panel/frontend/src/pages/admin/Packs.tsx
tags: [deferred-plan, v4.19.1, v4.19.2, v4.19.3, v4.19.4, v4.19.5, setup-wizard, deprecated-cleanup, gm-workbench, code-cleanup, feature-gap, test-matrix, architecture]
---

# v4.19.1+ 推迟实施计划

## 一、背景

v4.19.0 严格按 [docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §十二小版本号路线图执行，完成了 M5（Schema 基线重置）+ M2（R3 鉴权链路遗留项修复）。其余模块（M1/M3/M4/M6/M7/M8/M9）按方案设计推迟至后续小版本。

本文档承接 v4.19.0 未完成内容，按方案 §十二路线图拆分至 v4.19.1 ~ v4.19.x 各小版本，并补充 v4.19.0 验证过程中发现的预先存在测试失败修复。

## 二、版本路线图

| 小版本 | 模块 | 内容 | 依赖 | 状态 |
|--------|------|------|------|------|
| **v4.19.1** | M1 + 测试修复 | Setup Wizard 收尾 + 前端预先存在测试失败修复 | v4.19.0 | ✅ 已完成 |
| **v4.19.2** | M3 + M4 | 过渡期字段删除 + GM Workbench 玩家操作按钮 | v4.19.1 | ✅ 已完成 |
| **v4.19.3** | M6 + M3.4 收尾 | 代码清理与文档维护（D1-D6 + D10-D12）+ PlayerBindingSummary 物理删除 | v4.19.2 | ✅ 已完成 |
| **v4.19.4** | M7 | 功能缺口补齐（D7-D9） | v4.19.3 | 待启动 |
| **v4.19.5** | M8 | 正式功能测试矩阵校准与执行 | v4.19.4 | 待启动 |
| **v4.19.x** | M9 | 远期架构改进（D13-D16 + D18） | 不限 | 远期 |

---

## 三、v4.19.1：M1 Setup Wizard 收尾 + 前端测试修复

### 3.1 M1 Setup Wizard 收尾

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §三 M1

#### 3.1.1 实施状态（v4.19.1 已完成）

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | public/ 契约层（类型 + 接口签名） | ✅ v4.19.0 已落地 |
| 2 | 密码策略公开化 | ✅ v4.19.0 已落地 |
| 3 | Seed 门控收紧 | ✅ v4.19.0 已落地 |
| 4 | Preflight 接口 | ✅ v4.19.0 已落地 |
| 5 | 前端 API client 扩展 | ✅ v4.19.0 已落地 |
| 6 | SetupWizard.tsx 5 步流程 + BUILD footer | ✅ v4.19.0 已落地 |
| 7 | 后端单测/集成测试 + 前端组件测试 | ✅ v4.19.1 已闭合（settings.test.ts 50 用例 + SetupWizard.test.tsx 31 用例） |
| 8 | 版本号同步 + 文档 | ✅ v4.19.1 已完成（package.json/version.json/deploy.sh/version.md 全部对齐） |
| 9 | migration `20260808000001_system_mode_and_preflight.ts` | ✅ v4.19.0 已存在 |
| 10 | PanelApiError details 字段透传（关键修复） | ✅ v4.19.1 已修复（WEAK_PASSWORD 现可展示字段级失败清单） |
| 11 | system.mode + system.preflight_passed schema 定义 | ✅ v4.19.1 已补齐（settingSchemaService.ts 新增 system group） |
| 12 | Demo 模式 needsInit 门控绕过 | ✅ v4.19.1 已修复（settings.ts 增 isDemoMode 短路） |

#### 3.1.2 v4.19.1 执行步骤

##### 后端测试补齐

- [ ] `panel/backend/src/services/initPreflightService.test.ts`：8 项 preflight 检查单测
  - database / migrations / daemon / packs / db_config / mode / disk / public_url
  - 每项检查覆盖 success / warn / error 三种状态
- [ ] `panel/backend/src/api/routes/settings.test.ts`：扩展覆盖
  - `GET /api/init/preflight` 返回 8 项检查结果
  - `POST /api/init` 接受新字段（admin.email/username、mode、database_ack）
  - 弱密码硬拦截（zxcvbn score < 阈值 + forbidden 列表）
  - `detectInitStatus` 4 个条件（admin 改密 + site.name + system.mode + system.preflight_passed）
- [ ] `panel/backend/src/services/passwordPolicy.test.ts`：补充 `checkForbidden` 选项测试
  - 当前已有 13 个测试，需补充 `isForbiddenPassword` + `checkForbidden` 选项覆盖

##### 前端组件测试 + E2E

- [ ] `panel/frontend/src/pages/SetupWizard.test.tsx`：5 步流程组件测试
  - Step 0 preflight 检查列表渲染
  - Step 1 模式选择（演示/生产）
  - Step 2 站点信息
  - Step 3 管理员账号（含密码 zxcvbn 评分条）
  - Step 4 Pack 选择
  - Step 5 完成（跳转登录）
  - 弱密码硬拦截 + 错误提示
  - BUILD footer 渲染
- [ ] `panel/frontend/e2e/setup-wizard.spec.ts`：E2E 全流程
  - 访问 `/setup` → 完成 5 步 → 跳转登录
  - 含 warn/error 分支

##### 浏览器回归

- [ ] 浏览器实测 `https://gsp.ecsrz.com:3001/setup` 全流程
- [ ] 验证 BUILD footer 展示正确
- [ ] 验证弱密码提示
- [ ] 验证完成后跳转

##### 版本叙述对齐

- [ ] 更新 `version.md` 中 v4.18.0 条目，标注"v4.19.1 收尾闭合"
- [ ] 更新 `docs/plans/setup-wizard-fix-plan.md` 状态为 `deployed`

##### 部署验证

- [ ] 部署 v4.19.1 到生产环境
- [ ] 浏览器实测 `/setup` 流程通过
- [ ] 验证 `system_config` 表中 `system.mode` / `system.preflight_passed` 写入正确

### 3.2 前端预先存在测试失败修复

> 来源：v4.19.0 `npm run check` 验证发现，已通过 `git stash` 确认为 master 提交前已存在

#### 3.2.1 失败详情

**`src/pages/Login.test.tsx`（6 个失败）**：
- `Login 页面 > 渲染登录表单（标题、邮箱、密码、提交按钮）`
- `Login 页面 — demo 一键登录（3 级权限矩阵） > 测试环境（DEV=true）默认展示 3 个 demo 登录按钮`
- `Login 页面 — demo 一键登录（3 级权限矩阵） > VITE_ENABLE_DEMO=true 时展示 3 个 demo 登录按钮`
- `Login 页面 — demo 一键登录（3 级权限矩阵） > 点击 'server_admin' demo 按钮自动填表并完成登录跳转`
- `Login 页面 — demo 一键登录（3 级权限矩阵） > 点击 'instance_admin' demo 按钮自动填表并完成登录跳转`
- `Login 页面 — demo 一键登录（3 级权限矩阵） > 点击 'user' demo 按钮自动填表并完成登录跳转`

**`src/pages/guild/__tests__/GuildBind.test.tsx`（1 个失败）**：
- `GuildBind > 创建绑定：输入角色名后新增绑定卡片并提示验证码`：找不到匹配 `/创建绑定|立即.../` 的按钮

#### 3.2.2 根因分析（待 v4.19.1 详细排查）

初步判断为 v4.18.0 Setup Wizard 修复 + v4.17.0 三层入口重构（admin/store/guild）期间的测试断言漂移：
- Login 页面 demo 登录按钮渲染逻辑可能因 `from` 参数处理或 BUILD footer 改造而变化
- GuildBind 按钮文案/可见性可能因角色权限或绑定状态门控而变化

#### 3.2.3 修复步骤

- [ ] 排查 `Login.test.tsx` 6 个失败的具体根因
  - 检查 `Login.tsx` 当前渲染逻辑（demo 按钮显示条件）
  - 检查测试 mock 是否与当前 API client 签名一致
  - 必要时更新测试断言或修复组件渲染逻辑
- [ ] 排查 `GuildBind.test.tsx` 1 个失败的具体根因
  - 检查 `GuildBind.tsx` 中"创建绑定"按钮的渲染条件
  - 检查测试 mock 数据是否触发按钮显示
  - 必要时更新测试断言或修复组件渲染逻辑
- [ ] 验证 `npm run check` 全部通过

---

## 四、v4.19.2：M3 过渡期字段删除 + M4 GM Workbench 按钮

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §五 M3 + §六 M4
> M3 + M4 [P] 可并行

### 4.1 M3：过渡期字段删除

#### 4.1.1 `users.role` 代码引用清理

> **M5 基线吸收**：`users.role` 列的 DROP COLUMN 操作已被 M5 基线脚本吸收（建表语句不创建此列）。M3 仅需处理代码层和契约层的清理。

| 文件 | 动作 |
|------|------|
| `public/schema/user-schema.json` | 删除 `role` 字段定义 |
| `public/schema/panel-api-types.ts` | 删除 `MeResponse.role` / `User.role` 等 `@deprecated` 字段 |
| `public/interface_stub/shared-types.d.ts` | 删除 `@deprecated` 的 `role` 字段 |
| `panel/backend/src/core/auth/jwt.ts` | 删除 JWT payload 中的 `role` 字段 |
| `panel/backend/src/services/userService.ts` | 删除 `toUser()` / `toUserInfo()` 中 `role: updated.active_role` 的回填逻辑 |
| `panel/backend/src/middleware/auth.ts` | 删除 `req.user.role` 回退逻辑 |
| `panel/backend/src/api/routes/users.ts` | 删除响应中的 `role` 字段 |
| `panel/backend/src/routes-registry.ts` | 删除 `@deprecated` 标注的 role 引用 |

#### 4.1.2 全量影响面排查（M3 不能只按上表点名文件执行）

- [ ] Grep 全量排查前端/后端中 `role` 旧字段引用（排除 `roles` / `active_role`）
- [ ] 后端 DAO 层与 `userService.ts` 严格审查，防止 `SELECT *` 导致存量 SQLite `role` 幽灵列向前端泄漏
- [ ] `panel/backend/src/services/playerService.ts` / `panel/backend/src/api/routes/playerBindings.ts`：确认并迁移 `PlayerBindingSummary` 使用方
- [ ] 前端页面与组件（含 guild/store 相关页面）全量排查 `PlayerBindingSummary` / 旧 `role` 字段依赖
- [ ] 调用 s0202 重新基于最新接口存根生成全量 Mock 数据（`public/pre_generated_mock/`）
- [ ] 更新 `public/pre_generated_mock/`、Vitest fixture、Playwright/E2E 帮助函数，去除旧 `role`/旧账号假设

#### 4.1.3 `player-bindings-schema.json` 删除

- [ ] **public/ 保护门控**：删除前需经 `ec7_action_gate`（rules-0 §四-7.2）+ 人类显式授权（rules-0 §四-10）
- [ ] 物理删除 `public/schema/player-bindings-schema.json`
- [ ] 移除所有 import 引用
- [ ] 确认 v4.17.0 阶段D+ 已将业务代码全部迁移到统一 `bindings` 表，旧 schema 已无运行时引用

#### 4.1.4 `PlayerBindingSummary` 类型删除

- [ ] 删除 `public/schema/panel-api-types.ts` 中的 `PlayerBindingSummary` 类型定义
- [ ] 删除所有引用
- [ ] 删除前确认玩家绑定链路已全面切换到统一 `Binding` 契约

#### 4.1.5 验证

- [ ] `npm run verify`（前后端 type-check + 单测全绿）
- [ ] API 响应不再含 `role` 字段
- [ ] `player-bindings-schema.json` 已物理删除

### 4.2 M4：GM Workbench 玩家操作按钮

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §六 M4

#### 4.2.1 后端已有 API（无需改动）

| 端点 | 功能 | 文件 |
|------|------|------|
| `POST /api/store/players/:userId/compensate` | 发放补偿（RCON give） | `store-player-actions.ts` |
| `POST /api/store/players/:userId/ban` | 封禁玩家（RCON ban） | `store-player-actions.ts` |
| `POST /api/store/players/:userId/adjust-playtime` | 调整 VIP 时长 | `store-player-actions.ts` |

#### 4.2.2 前端改造

**文件**：`panel/frontend/src/pages/store/Players.tsx`

当前状态：展示玩家列表（用户名/邮箱/游戏ID/状态/VIP/消费/时长/绑定时间），无可操作按钮。

改造内容：
1. 新增操作列（`DataTableColumn`），每行显示 3 个按钮：
   - **发放补偿**（Gift 图标）→ 弹出 CompensateModal（选择物品名+数量+原因）
   - **封禁玩家**（ShieldOff 图标）→ 弹出 BanConfirmModal（填写封禁原因，二次确认）
   - **调整时长**（Clock 图标）→ 弹出 AdjustPlaytimeModal（输入 delta_seconds，正数延长/负数缩短）
2. 每个 Modal 调用对应的 `api.compensatePlayer` / `api.banStorePlayer` / `api.adjustPlayerPlaytime`
3. 操作完成后 `toast.success` + 自动 `refresh()` 列表

#### 4.2.3 新增文件

- [ ] `panel/frontend/src/pages/store/components/CompensateModal.tsx`：新建（补偿发放弹窗）
- [ ] `panel/frontend/src/pages/store/components/BanConfirmModal.tsx`：新建（封禁确认弹窗）
- [ ] `panel/frontend/src/pages/store/components/AdjustPlaytimeModal.tsx`：新建（时长调整弹窗）

#### 4.2.4 现有文件修改

- [ ] `panel/frontend/src/pages/store/Players.tsx`：新增操作列（3 按钮 → 3 Modal）+ `refresh()` 回调

#### 4.2.5 验证

- [ ] 前端 type-check + build 通过
- [ ] 浏览器验证玩家列表页显示"发放补偿/封禁/调整时长"按钮
- [ ] 3 个操作按钮 Modal 可正常提交并刷新列表

---

## 五、v4.19.3：M6 代码清理与文档维护

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §八 M6

### 5.1 死代码删除

- [D1] 删除 `panel/frontend/src/components/PlaceholderPage.tsx`（58 行，无任何引用）
- [D2] 确认 `panel/frontend/src/pages/Shop.tsx` 处置（649 行，lazy import 已注释，无路由注册）→ 删除或恢复

### 5.2 过期注释清理

- [D3] `App.tsx` L108+L415：更新 "v4.12.0: 占位，后端接入待 v4.13.0" → 当前状态
- [D4] `Login.tsx` L125：删除"身份 API 接入规划代码"TODO 注释块

### 5.3 文档状态维护

- [D5] 更新 3 个已部署方案的 frontmatter `status`：
  - `three-tier-operation-logic-rebuild-plan.md`
  - `v4.13.0-instances-split-plan.md`
  - `pack-system-overhaul-plan.md`
  - `planning` → `deployed`
- [D6] 归档 `v4.11.0-completion-migration-plan.md` → `docs/archive/`
- [D10] 勾选 4 个 `modules/模块N_*/AGENTS.md` 闭合判据 checklist
- [D11] 7 个 `draft` 方案文档人类裁决（保留/归档/排期）
- [D12] 2 个 `pending-approval` 方案人类裁决

> **D11/D12 不阻塞 M7/M8**，可与后续版本并行处理。

---

## 六、v4.19.4：M7 功能缺口补齐

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §九 M7
> v4.19.3 完成时已对 D7-D9 进行现状调研，本节为 v4.19.4 详细实施步骤。

### 6.1 D7：Pack 多 Variant 展示 + 版本号差异化

> 需求来源：[docs/plans/pending-requirements.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/pending-requirements.md)
> 现状：[panel/frontend/src/pages/admin/Packs.tsx#L300-L344](file:///home/airxw/Documents/gsp/gameserver-panel/panel/frontend/src/pages/admin/Packs.tsx#L300-L344) 仅显示 `variant` 字符串字段，未区分 variant_type / version_source

#### 6.1.1 现状分析

- `pack.yaml` 的 `variant` 字段当前是字符串（如 "vanilla" / "tshock"），无结构化 type/display_name
- 前端 PackCard / PackDetail / Pack 选择步骤未渲染 variant 标签
- 版本号统一显示，无来源标注（SteamCMD buildid / GitHub Release tag / Mojang API）
- 影响：terraria 同时有 `terraria-vanilla` 与 `terraria-tshock`，用户难以区分

#### 6.1.2 执行步骤

**后端契约扩展**：
- [ ] 扩展 `public/schema/pack-schema.json`（如存在）或 `pack-schema.ts`：`variant` 字段从 string 改为 object `{ type: string, display_name: string, description: string }`，保留旧 string 形式的向后兼容（zod union）
- [ ] 新增 `version_source` 字段到 pack 元信息：枚举 `steamcmd_buildid` / `github_release` / `mojang_api` / `manual`
- [ ] `GET /api/packs` 与 `GET /api/packs/:id/versions` 响应中返回 `variant_type` 与 `version_source` 字段
- [ ] 更新 `panel/backend/src/services/packService.ts` 解析逻辑：若 pack.yaml 的 variant 为对象则取 `type`，为字符串则原样使用

**前端展示改造**：
- [ ] [panel/frontend/src/pages/admin/Packs.tsx](file:///home/airxw/Documents/gsp/gameserver-panel/panel/frontend/src/pages/admin/Packs.tsx) L319：将 `{p.variant}` 改为渲染 variant 标签（颜色按 type 区分：vanilla=蓝色、tshock=紫色、modded=橙色）
- [ ] PackCard 组件：增加 variant_type 角标 + 版本号来源 tooltip（`via SteamCMD` / `via GitHub Release`）
- [ ] PackDetail 组件：variant 区块展示 display_name + description
- [ ] 实例创建页 Pack 选择步骤：同一 game 的多 variant 提供切换入口与比较视图
- [ ] 版本管理页（VersionsPage）：版本号旁边显示来源标注

**pack.yaml 适配**：
- [ ] 更新 `packs/terraria-vanilla/pack.yaml`：variant 改为 `{ type: "vanilla", display_name: "原版", description: "Terraria 官方服务端" }`，version_source: `steamcmd_buildid`
- [ ] 更新 `packs/terraria-tshock/pack.yaml`：variant 改为 `{ type: "tshock", display_name: "TShock", description: "TShock Mod 服务端" }`，version_source: `github_release`
- [ ] 其他 pack 保持 string variant 形式（向后兼容）

**测试**：
- [ ] 后端单测：packService 解析对象 variant 与字符串 variant 两种形式
- [ ] 前端组件测试：Packs.tsx 渲染 variant 标签 + 版本来源 tooltip
- [ ] 浏览器验证：terraria 双 Pack 在管理页/创建页/版本页均有差异化展示

#### 6.1.3 验证清单

- [ ] `npm run verify` 全绿
- [ ] terraria-vanilla 与 terraria-tshock 在前端展示有明显视觉差异
- [ ] 版本号来源标注正确显示
- [ ] 旧 pack（string variant）不报错

---

### 6.2 D8：Friends.tsx 同实例玩家推荐功能

> 现状：[panel/frontend/src/pages/Friends.tsx#L367-L374](file:///home/airxw/Documents/gsp/gameserver-panel/panel/frontend/src/pages/Friends.tsx#L367-L374) 显示"推荐功能暂未开放"EmptyState
> 后端：[panel/backend/src/services/friendService.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/services/friendService.ts) 无 recommend/suggest 方法

#### 6.2.1 现状分析

- 好友系统基础功能已完备：sendRequest / acceptRequest / rejectRequest / listFriends / listPendingRequests / listOnlineFriends / getFriendshipStatus
- 在线判定窗口：last_login_at 在 5 分钟内视为在线（`ONLINE_WINDOW_MS = 5 * 60 * 1000`）
- 推荐功能完全缺失：无后端 API、无前端 UI、无数据模型
- 数据基础已具备：`bindings` 表 `scope_type='instance'` 记录了用户与实例的绑定关系，可用于查找同实例玩家

#### 6.2.2 执行步骤

**后端推荐 API**：
- [ ] 在 [panel/backend/src/services/friendService.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/services/friendService.ts) 新增 `listRecommendations(userId: string, limit?: number)` 方法：
  - 查询用户已绑定且 `verify_status='verified'` 的实例列表（`bindings` 表 `scope_type='instance'` AND `user_id=userId`）
  - 查询这些实例下其他已验证绑定的用户（排除自己 + 排除已是好友/已 pending 的用户）
  - 按"共同实例数"降序排序，限制返回前 N 条（默认 10）
  - JOIN users 表返回 username / vip_level / last_login_at
- [ ] 在 [panel/backend/src/api/routes/friends.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/api/routes/friends.ts) 新增 `GET /api/friends/recommendations` 端点：
  - 鉴权：`authenticateToken`
  - 调用 `friendService.listRecommendations(req.user.id)`
  - 响应：`{ recommendations: Array<{ user_id, username, vip_level, last_login_at, common_instances: number }> }`
- [ ] 在 [public/schema/panel-api-types.ts](file:///home/airxw/Documents/gsp/gameserver-panel/public/schema/panel-api-types.ts) 新增 `ListFriendRecommendationsResponse` 类型

**前端 UI 实现**：
- [ ] 在 [panel/frontend/src/api/client.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/frontend/src/api/client.ts) 新增 `listFriendRecommendations()` 方法
- [ ] 改造 [panel/frontend/src/pages/Friends.tsx](file:///home/airxw/Documents/gsp/gameserver-panel/panel/frontend/src/pages/Friends.tsx) L367-L374：
  - 移除 EmptyState "推荐功能暂未开放"
  - 替换为推荐列表卡片：每条显示 username / 共同实例数 / VIP 等级 / "加好友"按钮
  - 空状态：无推荐时显示"暂无可推荐的好友，可先绑定更多服务器"
  - Loading / Error 三态处理
  - 点击"加好友"调用 `api.sendFriendRequest(userId)` 后刷新推荐列表

**测试**：
- [ ] 后端单测：`friendService.listRecommendations` 覆盖空绑定 / 单实例 / 多实例 / 排除已是好友
- [ ] 后端路由测试：`GET /api/friends/recommendations` 鉴权 + 响应格式
- [ ] 前端组件测试：Friends.tsx 推荐区渲染 / 空状态 / 加好友交互
- [ ] 浏览器验证：绑定同一实例的两个用户互相出现在推荐列表

#### 6.2.3 验证清单

- [ ] `npm run verify` 全绿
- [ ] Friends.tsx 推荐区显示同实例玩家
- [ ] "加好友"按钮可正常发送请求并刷新列表
- [ ] 无绑定用户看到空状态提示

---

### 6.3 D9：`!uptime` 真实 uptime 支持

> 现状：[panel/backend/src/services/inGameCommandService.ts#L372-L405](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/services/inGameCommandService.ts#L372-L405) 使用 `servers.created_at` 近似运行时长
> 关键发现：**Daemon 已具备真实 uptime 字段**，无需扩展 Daemon 接口

#### 6.3.1 现状分析

- `handleUptime` 当前查询 `servers` 表的 `created_at` 字段（服务器记录创建时间，非实例启动时间）
- Daemon 侧已实现真实 uptime：
  - [daemon/src/instances/manager.ts#L192](file:///home/airxw/Documents/gsp/gameserver-panel/daemon/src/instances/manager.ts#L192)：`uptime: this.computeUptime(instance)`
  - [daemon/src/instances/types.ts#L26](file:///home/airxw/Documents/gsp/gameserver-panel/daemon/src/instances/types.ts#L26)：`started_at: string | null`（实例启动时间）
  - [daemon/src/instances/manager.ts#L200-L205](file:///home/airxw/Documents/gsp/gameserver-panel/daemon/src/instances/manager.ts#L200-L205)：`computeUptime` 计算 `Date.now() - started_at`
- Panel 通过 `daemonClient.getInstanceStatus(instanceId)` 或类似方法可获取 daemon 返回的 `uptime` 字段
- 修复方案：调用 daemonClient 获取真实 uptime，失败时降级到 `created_at` 近似（保留现有逻辑作为 fallback）

#### 6.3.2 执行步骤

**修改 handleUptime 方法**：
- [ ] 在 [panel/backend/src/services/inGameCommandService.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/services/inGameCommandService.ts) L372-L405 修改 `handleUptime`：
  - 先查 `servers` 表获取 `daemon_url` / `daemon_token` / `status`
  - 若 `status !== 'running'`，返回当前提示（不变）
  - 调用 `daemonClient.getInstanceStatus(serverId)` 获取 `{ started_at, uptime }`
  - 优先使用 daemon 返回的 `uptime`（秒数）
  - daemon 调用失败时降级：使用 `servers.created_at` 计算（保留现有逻辑，加日志告警）
  - 格式化输出：`运行时长: X天Y小时Z分钟`（增加天数显示，避免长时间运行只显示小时）

**daemonClient 接口确认**：
- [ ] 确认 [panel/backend/src/services/daemonClient.ts](file:///home/airxw/Documents/gsp/gameserver-panel/panel/backend/src/services/daemonClient.ts)（或类似文件）是否有 `getInstanceStatus` / `getInstance` 方法
- [ ] 确认响应类型包含 `uptime` 或 `started_at` 字段
- [ ] 若无对应方法，需扩展 daemonClient：调用 `GET /api/instances/:id/status`（daemon 端点已存在）

**测试**：
- [ ] 后端单测：`handleUptime` daemon 可达时返回真实 uptime
- [ ] 后端单测：`handleUptime` daemon 不可达时降级到 created_at 近似（日志告警）
- [ ] 后端单测：长时间运行（>24h）格式化含天数
- [ ] 游戏内验证：在运行中的实例发送 `!uptime` 命令，返回真实运行时长

#### 6.3.3 验证清单

- [ ] `npm run verify` 全绿
- [ ] `!uptime` 命令返回真实运行时长（与 daemon 进程实际启动时间对齐）
- [ ] daemon 不可达时降级逻辑生效（不抛错给用户）
- [ ] 长时间运行格式化正确

---

## 七、v4.19.5：M8 正式功能测试矩阵

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §十 M8

[D17] 先校准测试矩阵，再执行正式功能验证。当前矩阵仍以"3 内置账号（admin/manager/user）+ 旧角色名 `instance_manager`"为前置，与 M1 的生产 seed 模型及当前角色命名不一致。

### 7.1 执行前必须完成的校准

1. 将测试矩阵中的角色名统一从 `instance_manager` 校正为 `instance_admin`
2. 将"3 内置账号"拆分为两种执行场景：
   - **兼容旧生产数据场景**：沿用已存在的 `admin/manager/user`
   - **v4.19+ 标准生产场景**：仅 seed `admin@local.dev`，其余测试账号通过 provisioning 脚本或人工准备
3. 为 M8 增加"测试账号 provisioning"步骤，明确 manager/user/普通账号如何创建、是否为内置账号、是否允许改密
4. 将矩阵中的环境前置从 `v4.16.3` 基线改写为"与当前待测发布版本一致"

### 7.2 执行步骤

- [ ] 校准 `docs/plans/formal-feature-test-matrix.md`：版本基线、角色名、账号模型、执行前置
- [ ] 准备测试账号 provisioning 方案（兼容旧生产数据 / v4.19+ 标准生产）
- [ ] 执行正式功能测试矩阵

---

## 八、v4.19.x：M9 远期架构改进

> 来源：[docs/plans/v4.19.0-unified-release-plan.md](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md) §十一 M9
> 本期仅登记，具体方案待后续独立规划

- [D13] `AppError` 错误类体系重构（~180 处 `throw new Error()` 无结构化错误码）
- [D14] `store-gm.ts` 8 处 `any` 类型化
- [D15] business-logic-v1 下游：s0202 Mock 生成 + s0203 模块拆分
- [D16] P4 PostgreSQL 迁移（M5 基线已铺路）
- [D18] 内存 Token 黑名单的 Redis/DB 持久化改造，适配多节点集群（M2 遗留）

---

## 九、执行顺序与依赖 DAG

```
v4.19.0（已完成）
  ├── M5 Schema 基线重置 ✅
  └── M2 R3 鉴权链路修复 ✅

    ↓

v4.19.1: M1 Setup Wizard 收尾 + 前端测试修复 ✅
   ├── M1 后端测试补齐 ✅
   ├── M1 前端组件测试 + E2E ✅
   ├── M1 浏览器回归 + 部署验证 ✅
   ├── 前端 Login.test.tsx 6 失败修复 ✅
   └── 前端 GuildBind.test.tsx 1 失败修复 ✅

    ↓

v4.19.2: M3 (过渡期字段删除) + M4 (GM Workbench 按钮) [P] ✅
   ├── M3: users.role 代码清理 + player-bindings-schema.json 物理删除 ✅
   ├── M3.4: PlayerBindingSummary 标注 @deprecated（推迟物理删除至 v4.19.3）✅
   └── M4: 3 Modal + Players.tsx 接入 ✅

    ↓

v4.19.3: M6 代码清理与文档维护 + M3.4 收尾 ✅
   ├── D1-D6 死代码删除 + 过期注释清理 + 文档状态维护 ✅
   ├── D10 模块闭合判据勾选 + 运行时接入证据补录 ✅
   ├── D11-D12 方案文档 frontmatter status 更新 ✅
   └── M3.4 收尾: PlayerBindingSummary 物理删除 + 全量迁移到统一 Binding 契约 ✅

    ↓

v4.19.4: M7 功能缺口补齐（待启动）
   ├── D7 Pack 多 Variant 展示 + 版本号差异化
   ├── D8 Friends.tsx 同实例玩家推荐功能
   └── D9 !uptime 真实 uptime 支持（daemon 已具备字段，仅需 Panel 接入）

    ↓

v4.19.5: M8 正式功能测试矩阵（待启动）
   ├── 校准 formal-feature-test-matrix.md
   ├── 准备测试账号 provisioning
   └── D17

    ↓

v4.19.x: M9 远期架构改进（远期）
   └── D13-D16 + D18（按需拆子版本）
```

---

## 十、风险与建议

### 10.1 风险

1. **v4.19.1 前端测试修复根因不明**：Login/GuildBind 测试失败的根因需详细排查，可能涉及组件渲染逻辑变更或测试 mock 漂移。若根因复杂，可能影响 v4.19.1 闭合时间。

2. **v4.19.2 M3 public/ 删除操作需授权**：删除 `player-bindings-schema.json` 和修改 `user-schema.json` 需经 `ec7_action_gate` + 人类显式授权。

3. **v4.19.2 M3 全量影响面排查不彻底**：若任何代码路径仍有 `role` 回退逻辑（如 `row.role ?? row.active_role`），M5 基线新安装不创建此列，但存量部署的列仍存在 → 可能静默读到旧值。**必须**先完成 M2 鉴权链路加固（v4.19.0 已完成）+ Grep 全量排查。

4. **v4.19.5 M8 测试账号 provisioning 复杂**：兼容旧生产数据 + v4.19+ 标准生产两种场景需准备两套测试账号，provisioning 脚本设计需谨慎。

### 10.2 建议

1. **v4.19.1 优先闭合**：M1 Setup Wizard 收尾 + 前端测试修复是 v4.19.x 后续所有版本的基础，应优先完成。
2. **v4.19.2 M3 + M4 并行**：M3（代码/契约清理）和 M4（前端 UI）改动独立，可并行推进。
3. **v4.19.3 D11/D12 人类裁决**：9 个方案文档的处置决策需用户参与，建议提前通知用户准备裁决。
4. **v4.19.x M9 远期改进**：D13-D16 + D18 涉及架构级重构，建议每个 D 项独立规划版本。

---

## 十一、参考

- [v4.19.0 统一发布规划](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/v4.19.0-unified-release-plan.md)（主方案）
- [Setup Wizard 修复方案](file:///home/airxw/Documents/gsp/gameserver-panel/docs/plans/setup-wizard-fix-plan.md)（M1 详细方案）
- [GN-004 R3 审查记录](file:///home/airxw/Documents/gsp/gameserver-panel/.trae/documents/20260725_v4.17.0_GN-004_R3_鉴权链路审查.md)（M2 来源）
- [version.md](file:///home/airxw/Documents/gsp/gameserver-panel/version.md)（v4.19.0 变更说明）
- [rules-0 §四-7.2 ec7_action_gate](file:///home/airxw/Documents/gsp/gameserver-panel/.trae/rules/rules-0.md)（public/ 操作门控）
- [rules-0 §四-10 public/ 保护](file:///home/airxw/Documents/gsp/gameserver-panel/.trae/rules/rules-0.md)
- [bb.md 版本号规则](file:///home/airxw/Documents/gsp/gameserver-panel/.trae/rules/bb.md)
