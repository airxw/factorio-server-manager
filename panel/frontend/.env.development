# 开发环境（vite dev server）
# REST 走 Vite proxy，故留空
# WebSocket 需直连后端 3000 端口（proxy 的 ws 配置已处理 /ws 转发，故仍可留空）
# 如需显式指定后端，取消下行注释（注：严禁使用 localhost:3000 作为生产外网访问地址）：
# VITE_API_BASE=http://your-backend-address:port

# 开发环境默认展示 demo 登录入口（一键 admin@local.dev）
VITE_ENABLE_DEMO=true
